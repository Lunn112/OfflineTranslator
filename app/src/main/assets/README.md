Place converted NLLB ONNX files in an app-private language-pack directory at runtime.
The application never bundles or calls a translation cloud API. A real pack must include
model.onnx, tokenizer vocabulary and source/target language metadata.
